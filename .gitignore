# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
/bin/
/dist/

# Coverage
*.out
coverage.out
coverage.html

# Go workspace
go.work
go.work.sum
vendor/

# Catacomb local state
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Env / secrets
.env
*.pem
*.key

# Editors / OS
.vscode/
.idea/
*.swp
*~
.DS_Store

# Logs / temp
*.log
logs/
tmp/
.cache/
*.pprof

# Bare binary from `go build ./cmd/catacomb`
/catacomb

# E2E live gate output (manifests + regress json; see e2e/run.sh)
e2e-artifacts/

# SQL basket agent scratch — the sql cells write out/result.csv under e2e/ (dir: .);
# run.sh cleans it on exit, this catches a crashed run.
e2e/out/

# Subagent-driven-development scratch (ledger, briefs, reports, diffs) — not project docs
.superpowers/

# Playwright MCP scratch (screenshots/snapshots from direct MCP browser use)
.playwright-mcp/

# Claude Code local runtime (hooks point at machine-specific absolute paths)
/.claude/

# Python (integrations/)
integrations/**/.deepeval/
integrations/**/__pycache__/
integrations/**/.pytest_cache/
integrations/**/*.egg-info/
integrations/**/dist/
